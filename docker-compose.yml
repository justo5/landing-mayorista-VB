services:
  mntn-landing:
    build:
      context: .
    container_name: mntn-landing
    restart: unless-stopped
    networks:
      - npm_network

networks:
  npm_network:
    external: true
    name: mi-red-nginx
